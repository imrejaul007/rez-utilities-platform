name: Karma CI

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      mongodb:
        image: mongo:7
        ports: ['27017:27017']
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ping:1})'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Type check
        run: npx tsc --noEmit 2>/dev/null || echo "no typecheck"
      - name: Lint
        run: npm run lint 2>/dev/null || echo "no lint"
      - name: Build
        run: npm run build 2>/dev/null || echo "no build"
      - name: Security audit
        run: npm audit --audit-level=high 2>/dev/null || echo "no audit"
          continue-on-error: true
